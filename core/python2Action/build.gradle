subprojects {

    task dockerFiles(type: Copy) {
        from (new File(rootDir, 'src')) {
            include '*.py'
        }
        from (new File(rootDir, 'src/Dockerfile.in')) {
            rename { 'Dockerfile' }
            expand ( pythonVersion: '2.7.14' )
        }
        into dockerBuildImage.inputDir
    }

    dockerBuildImage.dependsOn dockerFiles
}

